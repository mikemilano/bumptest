{
  "name": "bumper",
  "version": "0.0.2",
  "description": "I am made for bumping.",
  "main": "index.js",
  "author": "Mike Milano",
  "license": "MIT",
  "scripts": {
    "release": "./node_modules/.bin/bump --prompt --commit --all --tag --push"
  },
  "dependencies": {
    "version-bump-prompt": "^4.0.0"
  }
}